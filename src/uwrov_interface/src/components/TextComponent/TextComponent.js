import React

class TextComponent extends React.Component {
  state
  render() {
    return (
      // Area for reading messages
      // Area to input messages
      // Button to enter your message
    )
  }
}
